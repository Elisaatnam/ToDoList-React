import { createContext } from "react";

export const ListeninhaltContext = createContext();

export const OpenEditorContext = createContext();

export const UpdateTextContext = createContext();

export const UpdateIndexContext = createContext();
